
- current_user.groups.each do |group|
  = link_to group_messages_path(group) do
    %p.chat-side__groups__group__group-name
      = group.name
    %p.chat-side__groups__group__group-message
      メッセージはまだありません。
